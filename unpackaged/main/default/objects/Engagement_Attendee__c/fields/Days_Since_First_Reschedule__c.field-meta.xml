<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Days_Since_First_Reschedule__c</fullName>
    <externalId>false</externalId>
    <formula>IF(
    NOT( ISBLANK( First_Reschedule_Date__c ) ),
    IF(
        ISBLANK( Cancellation_Date__c ),
        TODAY() - First_Reschedule_Date__c,
        Cancellation_Date__c - First_Reschedule_Date__c
    ),
    NULL
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Days Since First Reschedule</label>
    <precision>18</precision>
    <required>false</required>
    <scale>0</scale>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
