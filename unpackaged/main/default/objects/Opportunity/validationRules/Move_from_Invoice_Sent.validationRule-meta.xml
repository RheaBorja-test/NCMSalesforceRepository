<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Move_from_Invoice_Sent</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
   ISCHANGED(StageName),
   ISPICKVAL(PRIORVALUE(StageName), &#39;Invoice Sent&#39;),
   NOT(ISPICKVAL(StageName, &#39;Closed Lost&#39;)),
   NOT(ISPICKVAL(StageName, &#39;Closed Won&#39;)),
   NOT(ByPassValidation__c),
   $Profile.Name &lt;&gt; &#39;System Administrator&#39;
)</errorConditionFormula>
    <errorMessage>You can only manually move an opportunity to &#39;Closed Lost&#39; from &#39;Invoice Sent&#39;</errorMessage>
</ValidationRule>
